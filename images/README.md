# Images

Add or remove photos here, then run:
```bash
node ../refresh-gallery.js
```

Sub-photos (albums):
- Fast: name subs like `photo<number>s*.*` (e.g., `photo14s1.JPG`).
- Or edit `../gallery.json` and add files to the cover’s `subs` array.

